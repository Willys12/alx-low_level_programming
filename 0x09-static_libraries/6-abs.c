#include "main.h"
/**
*_abs - Calculates an absolute value
*@n: Value to be calculated
*Return: Interger
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
